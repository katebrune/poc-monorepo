{
  "name": "@poc/strings",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "version": "0.0.1",
  "scripts": {
    "build": "rimraf ./dist && tsc --build --force"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "typescript": "^5.2.2"
  }
}
